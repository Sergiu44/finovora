export enum TransactionType {
  Income = 1,
  Expense = 2,
}

export enum CategoryType {
  Income = 1,
  Expense = 2,
  Transfer = 3,
}

export enum TransactionTypes {
  Income = 1,
  Expense = 2,
  Transfer = 3,
}
